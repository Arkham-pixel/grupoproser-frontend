import React from 'react';
import FormularioCasoPropiedades from './FormularioCasoPropiedades.jsx';

/** Carga del módulo: ficha del cliente (como Complex / Express / FDM). */
export default function CargaPropiedades() {
  return <FormularioCasoPropiedades />;
}
